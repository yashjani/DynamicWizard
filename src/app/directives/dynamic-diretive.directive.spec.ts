import { DynamicDiretiveDirective } from './dynamic-diretive.directive';

describe('DynamicDiretiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDiretiveDirective();
    expect(directive).toBeTruthy();
  });
});
